rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /tables/{table} {
      allow get;
    }
    match /players/{player} {
      allow get;
      allow list;
    }
  }
}
